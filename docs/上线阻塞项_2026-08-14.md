# 上线阻塞项清单（2026-08-14）

> 起因：重写 zzup.org 的隐私政策 / 服务条款 / 安全页时，逐条对照代码核实每一句话。
> 下面 6 条是**法务文本已经这么写了、但代码还不是这样**的地方 —— 每一条都会让公开文档变成
> 不实陈述，其中两条会直接导致应用商店拒审。
>
> 公司主体：**zZuP!, Inc.**（Delaware）· 运营范围：**仅美国** · 最低年龄：**18**
>
> 网站侧文件在 `D:\zZuP! website\`，App 侧在本仓库。文中所有 `文件:行号` 均以
> 2026-08-14 的 `main` 为准。

---

## 优先级总览

| # | 阻塞项 | 严重度 | 工作量 | 归属 |
|---|---|---|---|---|
| 1 | 没有年龄门槛 | 🔴 文档已写死 18，代码零校验 | 小 | 后端 + 前端 |
| 2 | 没有举报功能 | 🔴 Apple 1.2 会拒审 | 中 | 后端 + 前端 |
| 3 | pet_memories 不可查看、不随删号清除 | 🔴 隐私政策自相矛盾 | 中 | 后端 + 前端 |
| 4 | app.json 申请了从不使用的后台定位权限 | 🟠 Apple 会拒审 | 极小 | 配置 |
| 5 | OpenAI DPA 未签 / Supabase 区域未确认 | 🟠 运维 | 极小 | 运维 |
| 6 | 匿名强度是 A 方案（挡不住改客户端） | 🟡 已在文档里如实披露 | 大 | 架构决策 |

建议顺序：**4 → 1 → 5 → 3 → 2 → 6**。第 4 条十分钟，第 1 条半天，做完这两条风险下降最多。

---

## 1. 🔴 没有年龄门槛

### 现状

`terms.html` 和 `privacy.html` 都已写死 **"You must be at least 18 years old"**。但代码里：

- `app/screens/onboarding/OnboardingScreen.tsx:70` —— `date_of_birth: birthday.trim()`
  生日是一个**自由文本输入框**，没有格式校验，没有下限判断
- 数据库侧：`profiles.date_of_birth` 是 `date` 类型（`25_user_profile_table.sql:34`），
  但没有任何 CHECK 约束或触发器验证年龄
- `docs/DEFERRED.md` 第 6 条明确记录了这个待办："18+ 年龄门槛 —— 服务端校验落点未定"

也就是说，现在输入 `2020-01-01` 也能注册成功。

### 要做什么

**服务端（权威，客户端绕不过）**

新建迁移 `71_age_gate.sql`：

```sql
-- profiles 的 BEFORE INSERT OR UPDATE 触发器
-- date_of_birth 非空时，拒绝不满 18 周岁的写入
create or replace function public.enforce_minimum_age()
returns trigger language plpgsql as $$
begin
  if new.date_of_birth is not null
     and new.date_of_birth > (current_date - interval '18 years') then
    raise exception 'You must be at least 18 to use zZuP!'
      using errcode = '23514';
  end if;
  return new;
end;
$$;

create trigger trg_enforce_minimum_age
  before insert or update of date_of_birth on public.profiles
  for each row execute function public.enforce_minimum_age();
```

⚠️ 注意：`delete_my_account`（迁移 67）会把 `date_of_birth` 置为 `null`，触发器要允许 null 通过，
上面的写法已经处理了。

**客户端**

- `OnboardingScreen.tsx` 的生日字段换成日期选择器，`maximumDate` 设为 18 年前的今天
- 捕获 `23514` 错误码，给出人话提示，而不是把 Postgres 报错原文弹给用户

### 验收标准

- [ ] 选择 17 岁生日 → 客户端就拦住，给出明确文案
- [ ] 直接调 `updateProfile({ date_of_birth: '2015-01-01' })` → 数据库拒绝
- [ ] 现有测试账号（00002 / 00005 / 00006）的生日字段不被误伤
- [ ] 删号流程仍然正常（触发器不阻挡置 null）

### 法务依据

守住 18 岁这条线，正是让 **California SB 243** 针对未成年人的三项额外义务（向未成年人披露 AI、
每 3 小时休息提醒、内容过滤）**不落到你们头上**的原因。这三项目前一项都没实现。
COPPA（13 岁以下）是联邦底线，同样绕不开。

---

## 2. 🔴 没有举报功能

### 现状

`lib/` 全目录 grep 不到任何 report 相关代码。目前用户举报的唯一途径是发邮件到 `admin@zzup.org`。

Apple 在 **2026 年 2 月**更新指南时明确：**随机 / 匿名聊天属于 UGC**，也就是 zZuPer Roam 和
zZuPer Pulse 全部落在指南 1.2 之下。1.2 要求四件事：

| 要求 | 现状 |
|---|---|
| 内容过滤机制 | ❌ 无 |
| **举报机制** | ❌ 无 |
| 屏蔽用户 | ✅ 有（`block_identity`，还是身份级的，做得比大多数产品细） |
| 24 小时内处理并移除违规者 | ⚠️ 流程未定 |
| 用户须同意禁止违规内容的条款 | ✅ 有 |

### 要做什么

**数据库** —— 新建迁移 `72_reports.sql`：

```sql
create table public.reports (
  id                uuid primary key default gen_random_uuid(),
  reporter_id       uuid not null references public.profiles(id) on delete set null,
  target_kind       text not null check (target_kind in
                      ('message','travel_post','travel_comment','user')),
  target_id         uuid not null,
  target_account_id uuid references public.profiles(id) on delete set null,
  reason            text not null check (reason in
                      ('harassment','sexual','minor_safety','violence',
                       'self_harm','spam','impersonation','other')),
  detail            text,
  status            text not null default 'open'
                      check (status in ('open','actioned','dismissed')),
  created_at        timestamptz not null default now(),
  resolved_at       timestamptz,
  unique (reporter_id, target_kind, target_id)   -- 同一对象不重复举报
);

alter table public.reports enable row level security;
-- 举报人只能看到自己提交的；写入一律走 RPC
create policy "see own reports" on public.reports
  for select using (auth.uid() = reporter_id);
```

配一个 `submit_report(p_target_kind, p_target_id, p_reason, p_detail)` 的
SECURITY DEFINER RPC，内部解析出 `target_account_id`（消息查 `messages.sender_id`，
漂流查 `travel_posts.user_id`，以此类推），客户端不传、也不能伪造。

**关键设计点**：`reports` 表**不能**因为被举报人删号而消失。
`delete_my_account`（迁移 67）文件头已经写了这个待办 —— 它引用了一个还不存在的
`docs/ACCOUNT_DELETION.md`。举证记录必须留存，理由和消息保留一样。

**前端入口**（至少这三处）

- `ChatScreen` —— 长按消息气泡 → 举报
- `OtherProfileScreen` —— `...` 菜单里加"举报此用户"
- `TravelDetailScreen` / `NearbyTravelScreen` —— 举报漂流内容或留言

**运营侧**

- 一个能看 `reports` 表的后台（哪怕先用 Supabase Dashboard 手工看）
- 明确谁在 24 小时内响应

### 验收标准

- [ ] 三个入口都能提交，重复举报同一对象被 unique 约束挡住
- [ ] 举报人看不到别人的举报；非举报人查不到任何行
- [ ] 被举报人删号后，`reports` 行仍在，`target_account_id` 变 null 但 `target_id` 保留
- [ ] 提交后有明确的成功反馈（"我们会在 24 小时内处理"）

### 法务依据

Apple App Store 指南 1.2。**这一条不做，iOS 提审必被拒。**
同时 `terms.html`、`guidelines.html`、`safety.html` 三处都写了"我们承诺 24 小时内处理举报"，
文档里我已经加了 `[Blocker: in-app reporting not shipped yet]` 标记，功能上线后要把这些标记删掉，
并把"应用内举报"写在邮箱前面。

---

## 3. 🔴 pet_memories 不可查看、不随删号清除

### 现状

这是整套合规里**最难解释**的一条。

`pet-chat` 每次对话后会异步跑一次额外的 LLM 调用，把用户说的话里的个人事实抽出来存库：

- 表定义：`supabase/migrations/56_pet_companion.sql:15` ——
  `pet_memories(id, user_id, summary, embedding, created_at)`
- 写入逻辑：`supabase/functions/pet-chat/index.ts:143-176`
- 存的内容长这样：`"Owner loves eating ramen"`、`"Owner is studying Computer Science"`

**这是一份由机器自动生成的用户画像，是不折不扣的个人信息。** 但是：

1. **没有任何界面能查看或删除它** —— 用户不知道宠物"记住"了什么，也没法纠正记错的内容
2. **`delete_my_account`（迁移 67）根本没碰这张表**。因为 profiles 是**软删除**，
   `pet_memories.user_id → profiles(id) ON DELETE CASCADE` 这条级联**永远不会触发**。
   结果：**用户删号之后，AI 写下的关于他的个人事实永久留存**
3. 同样的问题也存在于 `pet_chat_messages`（迁移 67 的注释里写了"删除策略待 Ethan 确认"）

隐私政策里我已经写明"你可以读取和删除宠物记忆"，`terms.html` 也这么写了 —— **目前这是假的。**

### 要做什么

**（a）纳入删号清理** —— 新建迁移 `73_delete_pet_data.sql`，或直接改 `delete_my_account`：

```sql
-- 在 delete_my_account 的第 1 层（抹除个人识别信息）之后加：
delete from public.pet_memories     where user_id = v_uid;
delete from public.pet_chat_messages where user_id = v_uid;
```

理由：这两张表**只对用户自己有意义**，不像 `messages` 那样属于对话双方，
也不构成任何举证价值（同样内容在 `messages` 里另有一份）。没有任何保留它们的正当理由。

**（b）查看 / 删除界面**

在 Profile 里加一个「你的宠物记得什么」页：

- 列出 `pet_memories.summary`，按时间倒序
- 每条可单独删除
- 一个"全部忘记"按钮
- 空状态文案要诚实：还没有记住任何东西

需要一个 `list_my_pet_memories()` 和 `delete_pet_memory(p_id)` 的 RPC，
或者直接靠现有 RLS（`56` 里的策略已经是 `auth.uid() = user_id` 的 FOR ALL，客户端可直接读写）。

**（c）顺带修一个 bug**

`agent-chat/index.ts` 有两处查 `pet_memories`：

- 第 171-176 行：`.select("memory_text")`
- 第 261 行：`.select("memory_text")`

但表里那一列叫 **`summary`**（见 `56_pet_companion.sql:18`）。这两个 select **必然报错**，
被 `?.` 静默吞成空字符串。也就是说 **Pulse 里"宠物带着主人的记忆去破冰"这个卖点从来没生效过**。
改成 `.select("summary")` 并相应改下面的 `.map((m) => m.memory_text)`。

### 验收标准

- [ ] 删号后 `select count(*) from pet_memories where user_id = '<已删账号>'` 返回 0
- [ ] `pet_chat_messages` 同上
- [ ] Profile 里能看到记忆列表，删除后再问宠物同一件事，它确实不记得了
- [ ] `agent-chat` 的 `memory_text` → `summary` 改完后，Pulse 开场白里能体现主人的记忆

### 法务依据

隐私政策「你的权利」一节承诺了 correct 和 delete，其中特别点名了
"including a pet memory the AI got wrong about you"。美国各州隐私法都有 delete 权，
删号后仍保留机器生成的用户画像**没有任何可援引的豁免**（安全 / 反欺诈 / 法规遵从都套不上）。

---

## 4. 🟠 app.json 申请了从不使用的后台定位权限

### 现状

`app.json` 里：

```jsonc
"android": {
  "permissions": [
    "android.permission.ACCESS_COARSE_LOCATION",
    "android.permission.ACCESS_FINE_LOCATION",
    "android.permission.ACCESS_BACKGROUND_LOCATION",   // ← 后台定位
    "android.permission.FOREGROUND_SERVICE",
    "android.permission.FOREGROUND_SERVICE_LOCATION"
  ]
},
"plugins": [
  ["expo-location", {
    "locationAlwaysAndWhenInUsePermission": "zZuP! uses your location to show friend locations.",
    "isIosBackgroundLocationEnabled": true,
    "isAndroidBackgroundLocationEnabled": true
  }]
]
```

但是：

- **`expo-location` 全项目零引用**（`grep -r "expo-location" --include=*.ts --include=*.tsx` 无结果）
- 那套地图 / 打卡 / 好友位置的功能在 6 月 v3 重构时**整套删掉了**（`user_locations`、
  `landmarks`、`explorations` 表全清），权限文案里的 "show friend locations" 指的就是这个已删功能
- `expo-notifications` 同样零引用（推送下发未建，见 `DEFERRED.md` #9）

**申请一个从不使用的权限 —— 尤其是后台定位 —— 是 Apple 审核的经典拒绝理由**
（指南 5.1.1：只能请求实现当前功能所必需的数据）。Google Play 对后台定位另有独立的
申报和审核流程，同样会卡住。

### 要做什么

1. 从 `app.json` 删掉整个 `expo-location` plugin 配置块
2. 删掉 android 的五条定位 / 前台服务权限
3. `npm uninstall expo-location expo-notifications`（确认真无引用后）
4. `npx expo prebuild --clean` 或直接重新 EAS Build，确认生成的 manifest 里没有残留

### 验收标准

- [ ] 构建产物的 `AndroidManifest.xml` 里搜不到 `ACCESS_BACKGROUND_LOCATION`
- [ ] iOS 的 `Info.plist` 里没有 `NSLocationAlwaysAndWhenInUseUsageDescription`
- [ ] App 启动和全流程功能不受影响

### 法务依据

隐私政策「What we do not do」一节明写 **"We do not collect your location. The app has no
GPS feature."** 只要 manifest 里还留着后台定位权限，这句话在审核员眼里就是不实陈述。

---

## 5. 🟠 OpenAI DPA 未签 / Supabase 区域未确认

纯运维，没有代码改动，但隐私政策里引用了这两件事。

### 要做什么

- [ ] **和 OpenAI 签 DPA**。隐私政策已经写了"我们与 OpenAI 之间有数据处理协议"
- [ ] 如果拿得到 **Zero Data Retention**，隐私政策里那句"OpenAI 保留 30 天用于滥用监控"
      要改成 ZDR 的表述（文档里已留标记）
- [ ] **在 Supabase Dashboard 确认项目 `ulrzilxhuuxxezhgrptg` 的区域在美国**，
      把 `privacy.html` 里 `[US region — confirm in the Supabase dashboard]` 换成具体区域名
- [ ] **把 App Store 和 Google Play 的发行区域限定为美国** ——
      不然"仅在美国提供"就只是一句声明，不是事实。一旦有欧盟用户进来，GDPR 立刻附着

---

## 6. 🟡 匿名强度是 A 方案

### 现状

`docs/DEFERRED.md` 第 10 条记录得很清楚：本期采用 **A 方案（API 层去标识化）** ——
库里照常存真实 `account_id`，`messages` / `conversation_members` 对成员开放直查（为了保住
Realtime），去标识化只靠读取 RPC。

弱点也写明了：**绕过 RPC 直查仍暴露 `account_id`，挡不住抓包 / 改客户端**，
无法对抗确定型攻击者跨窗口对照（宠物马甲 ↔ 真人账号）。

### 现在怎么处理的

隐私政策里没有粉饰，「How anonymous your pet actually is」一节按四个层次如实写了：

- 对普通用户：有效
- 对我们自己：完全不匿名（这是屏蔽和审核能成立的前提）
- **对技术攻击者：比看起来弱**，改客户端的人有可能把宠物身份关联回账号
- 对你自己：你说漏嘴了谁也救不了

### 要做什么（二选一）

- **A. 保持现状** —— 那就**一个字都别改**隐私政策里那段。绝不能把当前构建描述成强匿名
- **B. 上线前升级** —— 走 DEFERRED #10 里的 B 方案（纯 RPC 读取，`account_id` 永不出库，
  牺牲直连 Realtime）或 C 方案（每窗口不透明 `member_handle` 列，保住 Realtime）。
  工作量不小，涉及 `messages` / `conversation_members` 的 SELECT 权限、读取 RPC、
  以及 Realtime 推送层重构

选 B 的话，隐私政策那段要跟着重写。

---

## 附：网站侧还剩两个空

在 `D:\zZuP! website\`：

| 位置 | 占位符 | 需要 |
|---|---|---|
| `privacy.html` · `terms.html` | `[LAUNCH DATE]` | 生效日期 |
| `terms.html` | `[Define appeal SLA.]` | 申诉多久回复，建议 30 天 |

另外文档里散落着若干 `[Blocker: ...]` 注释，**对应功能上线后要逐个删掉**，
以及 `privacy.html` 末尾整节「Pre-launch blockers」是内部备忘，**发布前整节删除**。

还有一条已知问题（不阻塞上线，但会影响体验）：
`OnboardingScreen.tsx:30-35` 的头像预设用的是 **Unsplash 远程图**，
意味着每个新用户在注册流程里都会把 IP 暴露给第三方。建议换成本地打包图片。

---

## 附：本次核对中发现、但不属于阻塞项的 bug

留在这里避免以后重复排查：

1. **`agent-chat` 的 `memory_text` 列名错误** —— 见第 3 条 (c)
2. **Pulse 的 AI 自动轮转从未执行过** —— 云端没装 `pg_net` 扩展，且
   `get_supabase_internal_url()`（`60_matched_chat_v3_5.sql:129`）返回的是
   `http://kong:8000/...`，那是 Supabase CLI 本地 Docker 的网关地址，托管云端不存在。
   触发器每次抛异常，被 `exception when others then return new` 静默吞掉。
   实际效果：匹配成功后只有一条开场白，之后再无往来
3. **`agent-chat` 的默认学校写死 `UCL`** —— `agent-chat/index.ts:116`，
   `profile.university || "UCL"`。用户没填学校时，匹配向量里被塞进 "UCL" 这个噪声词。
   好在数据库层的同校加权用的是另一个参数（传的是真实空串），影响仅限向量噪声。改成 `|| ""` 即可
4. **`agent-chat` 与 `pet-chat` 的人格规则相反** —— `agent-chat` 的阶段指令明确要求
   `Include actions in asterisks`，而 `pet-chat` 明令禁止星号动作、强制拟声词。
   同一套宠物系统两个函数输出规则打架（外星人摇尾巴就是这么来的）
5. **`lib/api/travel.ts` 的 `createTemporaryDirectMessage`** 直接 insert
   `conversations` / `conversation_members`，与 `BACKEND_CONTRACT.md` 里"写只走 RPC"
   的约定冲突，未验证是否会被 RLS 拒绝

---

*2026-08-14 · 依据 zzup.org 法务文档改写时的逐条代码核对*
