-- 109 · get_my_profile 带回封禁状态
--
-- 被封的人**登得进来**（107 有意的设计），所以 App 必须知道自己是什么状态，
-- 否则界面上只会看到「发消息失败」，不知道为什么、到什么时候、怎么申诉。
--
-- 只在读自己时返回。get_other_profile 不返回 —— 别人是不是被封了不关你的事，
-- 而且能读到就能扫出全站的封禁名单。
--
-- 完整定义以线上为准。
